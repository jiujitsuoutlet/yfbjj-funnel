# Funnel release and verification

Date: 2026-09-12 America/Chicago (2026-09-13 UTC).

## Released

- Public site: https://welcome.yogaforbjj.net/
- Production Worker version: `64766b3e-11ba-4b6b-b284-2fc417128238`, 100% traffic.
- Release code: `110be71`, plus recovery commits `6b0a463`, `b42c90f`, `61f1092`.
- Previous version: `46d3882f-cb47-42a5-8e63-5bf8505d56e9`.
- Staging proof version: `ee997f0f-357a-4ed9-aed6-f0e6ad126151`.

Production preflight passed. Wrangler uploaded and activated the release, then
failed its existing zone-route inspection with Cloudflare code 10000. The token
does not have the needed zone Workers Routes read permission (and User Details
Read is also absent). No route or permission workaround was attempted. The
account deployment listing independently confirms the new version at 100%, and
the custom domain serves the new code. Health returned 200, connected D1, and all
12 tables. The video endpoint returned 206 `video/mp4` with a valid byte range.

## Fixes

- Preserve the published editor content and single-column offer designs; fix
  spacing, clipping, and mobile text that had shrunk below readable sizes.
- Serve the approved introduction video through the fixed first-party route.
- Carry the email from the initial form into Stripe Checkout.
- Claim each upsell before charging, retry with stable idempotency, recover
  successful payments after interruption, and prevent concurrent declines.
- Handle current Stripe Basil invoice data and bank-confirmation fallbacks.
- Recover final thank-you pages for direct-payment and subscription purchases.
- Ignore other-environment webhook purchases before writing orders.
- Use the permanent canonical login and Programs links on the access page.
- Keep mock subscription identifiers out of AutoCreator's real Stripe references.

## Actual no-charge buyer journeys

Two new disposable buyers were created only for this release proof.

1. Guard bundle plus $9 bump: real Stripe Checkout subtotal $23, total $0 using
   the isolated QA coupon. Correct lifetime sales page, accepted lifetime, correct
   certification page, accepted certification, correct fulfilled access page.
   No repeated Stripe Checkout for either QA upsell. Exact five bundle grants
   and active lifetime membership were read back from AutoCreator.
2. Guard only: real Stripe Checkout subtotal $14, total $0. Declined Head to Toes,
   declined lifetime, accepted monthly, declined certification, reached the
   correct fulfilled access page. Active monthly membership and Guard bundle
   were read back. The first monthly proof failed because AutoCreator rejected a
   synthetic subscription ID; after fixing that QA boundary, the SAME pending
   order recovered through the buyer button without another purchase.

Both inboxes received the actual AutoCreator setup emails (three for buyer one,
two for buyer two). The first buyer signed in through its delivered email, opened
the Level 1 instructor course and its first lesson, and played the 2:58 lesson to
100%. All three instructor collections and first lessons passed authenticated
access checks. Their ordered lesson counts are 12, 28, and 39. The main site's
teacher-training filter appeared empty, but the courses were available through
the observed Programs catalog; the funnel now links there directly.

## Verification record and limits

- Final automated suite: 93 passed, zero failed. Includes actual SQLite
  interruption/retry/webhook-recovery tests, plus mocked Stripe/AutoCreator tests.
- Secret scan, production preflight, and diff check passed.
- Ten rendering runs attempted in this work session: nine completed, one aborted.
  Initial run 1 found three undersized text findings; fixed. Run 2 was clean.
  Run 3 stopped on a network-idle wait (video streaming), so the harness was fixed
  to wait for actual asset readiness. Runs 4, 5, and 6 were clean.
  After further code changes, final-1 was clean but was not counted as a final
  clean sequence because the monthly QA defect was subsequently found.
  After that last fix, final-2, final-3, and final-4 each completed 12 pages at
  375, 768, and 1440 pixels: 36 checks each, zero failures. These final three
  rendering passes were consecutive and clean. Their machine reports are in
  `release-2026-09-12/`.
- A stale test assertion initially expected the old login domain; updated it to
  verify the canonical login, Programs link, and expired-email explanation.
- Actual production browser: both landing variants at 375 and 1024 pixels,
  no horizontal overflow or browser errors; landing body 16px, small notice14px.
  Introduction video reached readyState4 with a 22.51-second duration.
- Actual production Stripe created $14 and $23 unpaid Checkout sessions with the
  buyer email prefilled and the success URL on the production `/offer` route.
  Both sessions were immediately expired. No card entered and no money moved.
- Production webhook now includes `payment_intent.succeeded` and `invoice.paid`
  in addition to its four existing Checkout events.

The three-pass rendering result is NOT a claim of three paid end-to-end tests.
No real saved-card charge, bank authentication challenge, or 30-day renewal was
executed because the operator explicitly declined real-money testing. Those
paths have automated coverage, not live bank proof. QA upsells intentionally
grant access without creating real charges or recurring subscriptions.

AutoCreator's single-use setup emails still expire after 24 hours. Purchased
access does not expire with that email; the permanent login page can issue a
fresh link. The email token itself was not changed to last forever.

## Cleanup

The two release mock members were soft-deleted using AutoCreator's reversible
delete operation, and their six bundle grants were revoked. No real customer was
changed. Their audit ledger records remain intact. The temporary release coupon
was removed and the staging QA proof secret was removed; no-charge checkout is
closed. The staging config in source retains its original expired coupon.

Remaining operational housekeeping: expand the deployment token's zone Workers
Routes read permission so future deployments can finish route inspection without
the warning. The existing live route and this release are independently verified.
