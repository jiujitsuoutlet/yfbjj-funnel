document.getElementById('login-form').addEventListener('submit',async(event)=>{event.preventDefault();const status=document.getElementById('login-status');status.textContent='Signing in';const response=await fetch('/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:new FormData(event.currentTarget).get('password')})});if(response.ok){location.href='/admin/editor';return}const body=await response.json().catch(()=>({error:'login_failed'}));status.textContent=body.error==='rate_limited'?'Too many attempts. Try again later.':'Unable to sign in.';});
